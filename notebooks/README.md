# Public demonstration notebooks

These notebooks are lightweight public demonstrations that run on the repository's synthetic example tables and compact summary outputs. They are not full reproductions of archival builds that required the original public source files.

Run them from the repository root in numerical order.
